# Video-analytics extras (stage 1: person detection + tracking).
# Everything here is Apache-2.0 / BSD / MIT — no copyleft; see vision/README.md.
onnxruntime>=1.17     # runs YOLOX + RTMPose (ONNX) on the CPU; onnxruntime-gpu for CUDA
numpy
opencv-python>=4.10
yt-dlp>=2024.8
lap>=0.5              # linear assignment used by ByteTrack
